import random

rng = random.Random()
number = rng.randrange(1, 1000)

guesses = 0
msg = ""

# Write a guessing game which asks the user for input and then responds if the guessed the correct number
# Tell the guesser if their number is too high, too low or just right, then re-prompt the
# user for a number if it is incorrect

# *** Write your code here ***